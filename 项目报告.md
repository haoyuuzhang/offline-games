# 离线像素游戏厅 — 项目报告

## 一、项目简介

**离线像素游戏厅**（Offline Pixel Arcade）是一个纯静态的浏览器端像素风格小游戏合集。整个项目无需安装任何依赖、无需联网，可以直接从本地文件打开使用，也可以部署到任意静态文件服务器上。项目面向不需要网络环境的休闲娱乐场景，适合放在 U 盘、移动硬盘或无网络的电脑上使用。

### 核心特点

- **完全离线**：零网络请求、零 CDN、零外部依赖
- **即开即用**：双击 `open-offline.bat` 即可在 Chrome 中打开，或直接将 `index.html` 拖入浏览器
- **像素复古风格**：暗色主题 + 霓虹色彩 + 像素网格的视觉设计
- **键盘 + 触控双操控**：支持键盘（方向键/WASD）和触屏按钮两种操作方式
- **可测试**：核心游戏逻辑与渲染分离，可在 Node.js 环境下无浏览器进行单元测试

---

## 二、功能介绍

### 2.1 游戏列表

项目内置五款经典像素风格小游戏：

| 游戏 | ID | 操作方式 | 玩法简介 |
|------|-----|---------|---------|
| **贪吃蛇** | `snake` | 方向键 / WASD | 控制蛇吃食物变长，撞墙或撞自己即结束，速度随得分增加而加快 |
| **2048** | `twenty48` | 方向键 / WASD | 滑动合并相同数字方块，目标达到更高数值，无有效移动时结束 |
| **打砖块** | `breakout` | 左右方向键 | 移动挡板反弹小球消除砖块，全部消除后自动重置 |
| **像素躲避** | `dodger` | WASD / 方向键 | 操控方块躲避从天而降的彩色障碍物，生存时间越长得分越高 |
| **飞机大战** | `shooter` | WASD 移动 / 自动射击 | 自动发射子弹消灭敌机，击杀获得经验值升级，可从三个随机升级中选择强化 |

### 2.2 飞机大战升级系统

飞机大战是五款游戏中机制最为丰富的一款，包含完整的 Roguelite 成长系统：

- **8 种可叠加升级**：双发弹幕、穿甲弹、高速射击、推进器、装甲板、宽弹头、维修无人机、干扰器
- **经验值升级机制**：击杀敌机获得经验，经验达标时触发升级选择（三选一）
- **动态难度曲线**：敌人血量、体型和速度随玩家等级与游戏时间增长
- **生命值系统**：玩家拥有生命值，被敌机撞击扣血，归零游戏结束

### 2.3 用户界面

界面采用经典的左栏导航 + 中央游戏区的布局：

- **左侧边栏**：品牌标题 + 游戏列表（含操作提示）
- **中央游戏区**：
  - 顶部状态栏：当前游戏名称、得分、等级、提示信息
  - Canvas 画布：游戏渲染画面（960×640 内部分辨率，支持高 DPI 缩放）
  - 底部控制栏：开始/暂停/重置按钮 + 虚拟方向键触控板
- **中文界面**：界面文字为中文，体现本土化设计

---

## 三、项目架构

### 3.1 整体架构图

```
离线像素游戏厅
├── index.html              ← UI 外壳（布局、样式、DOM 结构）
├── arcade.js               ← 构建产物（单文件打包）
├── build.js                ← 构建脚本（将 src/ 模块合并）
├── open-offline.bat        ← Windows 离线启动器
├── README.md               ← 使用文档
├── tests/
│   └── arcade.test.js      ← 单元测试（Node 原生 assert）
└── src/
    ├── shared/
    │   ├── rng.js          ← 确定性伪随机数生成器
    │   └── render.js       ← Canvas 渲染工具函数
    ├── games/
    │   ├── snake.js        ← 贪吃蛇
    │   ├── twenty48.js     ← 2048
    │   ├── shooter.js      ← 飞机大战（含升级系统）
    │   ├── breakout.js     ← 打砖块
    │   └── dodger.js       ← 像素躲避
    └── registry.js         ← 游戏注册表 + 街机主控逻辑
```

### 3.2 分层设计

项目采用清晰的三层架构：

```
┌──────────────────────────────────────┐
│          UI 层 (index.html)           │
│  DOM 布局 · CSS 样式 · 事件绑定入口   │
└──────────────┬───────────────────────┘
               │ 调用
┌──────────────▼───────────────────────┐
│       控制层 (registry.js)            │
│  bootArcade() · 游戏切换 · 输入分发   │
│  游戏循环 · Canvas 自适应 · 状态同步  │
└──────────────┬───────────────────────┘
               │ 工厂模式创建
┌──────────────▼───────────────────────┐
│        游戏逻辑层 (src/games/)         │
│  贪吃蛇 · 2048 · 飞机大战 · 打砖块     │
│  像素躲避                             │
│  每个游戏暴露统一接口                   │
└──────────────┬───────────────────────┘
               │ 依赖
┌──────────────▼───────────────────────┐
│       共享工具层 (src/shared/)         │
│  createRng() · COLORS · drawGrid()   │
│  drawPixelRect() · drawOverlay()     │
└──────────────────────────────────────┘
```

### 3.3 统一游戏接口

每个游戏模块通过工厂函数 `createXxxGame(options)` 创建游戏实例，所有游戏实例遵循相同的接口契约：

```javascript
{
  id: string,           // 游戏标识
  title: string,        // 显示名称
  reset: function(),    // 重置游戏状态
  input: function(action, active?),  // 接收输入指令
  update: function(dtMs),  // 更新游戏逻辑（时间步长，毫秒）
  draw: function(ctx, canvas),  // 绘制到 Canvas
  getState: function(),    // 导出完整游戏状态（供测试用）
  getStatus: function()    // 导出 UI 状态（得分、等级、提示）
}
```

这种接口设计实现了**游戏逻辑与渲染/输入的完全解耦**，使得：
- 游戏逻辑可脱离浏览器在 Node.js 中直接测试
- 新增游戏只需实现该接口并注册到 `createGameRegistry()`
- 通过 `options` 参数注入初始状态和随机种子，实现状态的完全可控和可复现

### 3.4 模块系统设计

项目使用了一种**同构模块包装模式**，使得每个源文件同时支持浏览器和 Node.js 两种运行环境：

```javascript
// 每个源文件的模块包装模式
(function () {
  // ---- DEPS ----
  // 浏览器环境从 window.Arcade 获取依赖
  // Node 环境通过 require() 获取依赖

  // ## IMPLEMENTATION ##
  // 实际逻辑代码

  // ## EXPORT ##
  // 浏览器环境：Object.assign(window.Arcade, api)
  // Node 环境：module.exports = api
})();
```

`build.js` 构建脚本通过识别 `## IMPLEMENTATION ##` 和 `## EXPORT ##` 标记，从各源文件中提取函数体，拼接为单个自执行函数 `arcade.js`，供浏览器直接加载。

### 3.5 核心设计决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 随机数生成 | 确定性线性同余生成器 (Lehmer RNG) | 支持通过种子复现游戏状态，保证测试可重复 |
| 渲染方式 | Canvas 2D API | 像素风格游戏的最高效渲染方式 |
| 游戏循环 | requestAnimationFrame + 时间步长 | 帧率无关的游戏逻辑更新 |
| 输入处理 | 抽象 action 字符串 | 将键盘和触控按钮统一映射为标准动作 |
| 测试框架 | 无框架，Node 原生 assert | 遵循零依赖原则 |
| 构建方式 | 简单字符串拼接 | 零配置，比 webpack/rollup 更透明可控 |

---

## 四、设计实现过程

### 4.1 设计阶段

项目始于明确的设计文档（[设计规格](docs/superpowers/specs/2026-07-05-offline-arcade-design.md) 和 [实施计划](docs/superpowers/plans/2026-07-05-offline-arcade.md)），在设计阶段明确了以下约束：

1. **零网络依赖**：不发起任何网络请求，不使用 CDN 资源
2. **本地文件可用**：必须支持 `file://` 协议直接打开
3. **键盘 + 触控**：同时支持桌面和移动端操控
4. **像素复古风格**：视觉上保持可读性和响应式适配
5. **核心逻辑可测试**：无需浏览器即可运行测试

初始设计包含五款游戏：贪吃蛇、俄罗斯方块、乒乓球、打砖块、像素躲避。实际实现中将俄罗斯方块替换为 2048，将乒乓球替换为飞机大战。

### 4.2 实施过程

实施遵循**测试驱动开发 (TDD)** 流程：

**任务一：编写核心逻辑测试** (`tests/arcade.test.js`)
- 先编写游戏注册表、贪吃蛇吃食物和碰撞、2048 合并、飞机大战射击和升级等测试用例
- 运行测试确认失败（红色阶段）

**任务二：实现游戏逻辑与 UI**
- 实现共享工具层 (`rng.js`, `render.js`)：确定性随机数生成 + Canvas 像素渲染工具
- 分别实现五款游戏的工厂函数
- 实现游戏注册表和控制中心 (`registry.js`)
- 构建 UI 外壳 (`index.html`)：响应式布局、Canvas 画布、侧栏导航、控制按钮、触控板
- 实现主循环 `bootArcade()`：游戏切换、输入分发、状态同步、Canvas 自适应
- 运行测试确认通过（绿色阶段）

**任务三：构建系统**
- 实现 `build.js`：将 `src/` 目录下的模块文件拼接为单一 `arcade.js`
- 确保构建产物既能用于浏览器加载，也能被 Node.js 的 `require()` 引用

**任务四：离线部署**
- 编写 `open-offline.bat` 启动脚本，自动检测 Chrome 安装路径
- 编写 `README.md` 使用文档

### 4.3 测试覆盖

测试文件 [tests/arcade.test.js](tests/arcade.test.js) 包含 15 个测试用例，覆盖：

- 游戏注册表的正确性
- 贪吃蛇：食物进食与生长、墙壁碰撞死亡
- 2048：方块合并与计分
- 飞机大战：子弹命中敌机消灭、升级系统触发、升级选择应用、敌机难度随等级增长
- 键盘映射：方向键和 WASD 的正确转换
- UI 文字：界面中文本地化验证
- 触控按钮：HTML 实体箭头标签稳定性
- 升级卡片 UI：布局居中、点击命中判定
- 持续输入：多键同时按下时的行为（如同时按左右）
- 离线包：启动器和 README 文档完整性

### 4.4 最终交付物

| 文件 | 用途 |
|------|------|
| [index.html](index.html) | UI 外壳，包含完整 CSS 和 DOM 结构 |
| [arcade.js](arcade.js) | 单文件游戏逻辑包（约 980 行） |
| [build.js](build.js) | 构建脚本 |
| [src/](src/) | 模块化源代码目录 |
| [tests/arcade.test.js](tests/arcade.test.js) | 单元测试（零依赖） |
| [open-offline.bat](open-offline.bat) | Windows Chrome 离线启动器 |
| [README.md](README.md) | 使用文档 |

---

## 五、技术亮点总结

1. **零依赖理念**：整个项目不依赖 npm 包、CDN、框架，真正做到了完全自包含
2. **双环境同构**：源代码通过巧妙的模块包装模式，同时支持浏览器直接加载和 Node.js `require()` 两种方式
3. **可测试性设计**：通过工厂模式 + 依赖注入（`options` 参数）+ `getState()` 导出接口，游戏逻辑完全可脱离浏览器测试
4. **确定性 RNG**：使用种子化的线性同余生成器，确保相同输入产生完全相同的游戏状态，为测试和回放提供了基础
5. **统一接口契约**：所有游戏遵循相同的 `{ reset, input, update, draw, getState, getStatus }` 接口，便于扩展新游戏
6. **像素美学**：通过 Canvas 2D 绘制 + `drawPixelRect` 函数中的高光条效果，营造出具有立体感的像素风格
7. **自适应渲染**：Canvas 支持 `devicePixelRatio` 高 DPI 缩放，保证在 Retina 屏幕上清晰显示
